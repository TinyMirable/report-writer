# AGENTS.md

## 项目目标

这个项目用于创建一个SKILL，使得智能体在使用该SKILL时可以阅读和理解已有项目内容，并撰写符合规范的课程设计报告

## 项目已有资源

## 路由地图

- `spec/` : 当前版本目标，skill范围和交付规格
- `docs/`:记录和项目相关的文件。如果有新增的文档，必须更新index.md
- [课程设计报告书模板.md](课程设计报告书模板.md) : 从docx版本直接复制过来的md版，便于Agent理解原始模板
- [课程设计报告书模板.docx](课程设计报告书模板.docx) : docx格式的报告书模板，去掉的格式说明文字和说明，只需要向指定章节下方填充正文

## 开工流程

写skill前先做这些事：

1. 用 `pwd` 确认当前目录。
2. 读取 `claude-progress.md`，了解最新已验证状态和下一步。
3. 读取 `feature_list.json`，选择优先级最高的未完成功能，如果为空先根据`spec`下的需求创建一个便于后续对话使用。

## 工作规则

- 一次只做一个功能。
- 不要因为“文档或者代码已经写了”就把功能标记为完成。
- 除非为了消除当前 blocker 的窄范围修复，否则不要扩大到其他功能。
- 实现过程中不要悄悄改弱验证规则。
- 优先依赖仓库里的持久化文件，而不是聊天记录。

## 必需文件

- `feature_list.json`：功能状态的唯一事实来源
- `claude-progress.md`：会话进度和当前已验证状态
- `session-handoff.md`：较长会话可选的交接摘要

## 完成定义

一个功能只有在以下条件都满足时才算完成：

- 目标行为已经实现
- 要求的验证真的跑过
- 证据记录在 `feature_list.json` 或 `claude-progress.md`
- 仓库仍然能按标准启动路径重新开始工作

## 收尾

结束会话前：

1. 更新 `claude-progress.md`
2. 更新 `feature_list.json`
3. 记录仍未解决的风险或 blocker
4. 在工作处于安全状态后，用清晰的提交信息提交

